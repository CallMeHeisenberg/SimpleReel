import { EventDispatcher } from "../observer/EventDispatcher";

export class SpinButtonModel extends PIXI.Container {

    constructor() {
        super();
    }
    
    private init() : void {
        //when button is clicked dispatch clickButton event
    }
}